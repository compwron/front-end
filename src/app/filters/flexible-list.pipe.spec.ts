import { FlexibleListPipe } from './flexible-list.pipe';

describe('FlexibleListPipe', () => {
  it('create an instance', () => {
    const pipe = new FlexibleListPipe();
    expect(pipe).toBeTruthy();
  });
});
